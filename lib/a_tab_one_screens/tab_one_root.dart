import 'package:flutter/material.dart';

class TabOneRoot extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tab One Root'),
      ),
    );
  }
}