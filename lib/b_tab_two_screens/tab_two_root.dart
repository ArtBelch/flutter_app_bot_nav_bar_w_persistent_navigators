import 'package:flutter/material.dart';

class TabTwoRoot extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tab Two Root'),
      ),
    );
  }
}