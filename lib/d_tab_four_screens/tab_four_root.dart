import 'package:flutter/material.dart';

class TabFourRoot extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tab Four Root'),
      ),
    );
  }
}